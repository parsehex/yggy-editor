// for now this is a little inflexible. options:
// -d [number] = Max dialogue tree depth
// -c [number] = Number of characters to create
// -b [number] = Number of backgrounds to create
// notes:
// -- Data that requires an image (only characters and backgrounds right now) will be pulled from a pool of those images in the `assets` directory in project root
